# RAG Frontend

React UI for document upload and chat.

## Setup

```bash
npm install
npm start
```

App runs at: http://localhost:3000

## Configuration

Set `REACT_APP_API_URL` in `.env` if backend is not on `http://localhost:8000/api/v1`
